@model IEnumerable<Project_Poineet.Setting>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bonus_count)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Minus_count)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Holiday_s)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bonus_count)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Minus_count)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Holiday_s)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
